schema_version: 1

name: "jboss/base-centos7"
version: "1.2"
from: "centos:7"
description: "Base image for all other Middleware container images which provides common set of tools, jboss user and home directory"

platform: centos:7

modules:
  repositories:
  - name: jboss.base.image.repo
    path: modules/image
  install:
  - name: jboss.base.image
